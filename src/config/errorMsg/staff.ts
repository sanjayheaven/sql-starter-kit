import { IErrorMsg } from "../../types"

export const UsernameShouldBeUnique: IErrorMsg = {
  msg: "Username should be unique.",
  code: 2001,
}








export const StaffNotExist = {
  msg: "Staff does't exist.",
  code: 2002,
}
